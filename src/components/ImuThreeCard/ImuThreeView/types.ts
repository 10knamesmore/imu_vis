/**
 * 轨迹显示配置选项
 */
export type TrajectoryOption = {
  /** 是否显示 X 轴端点轨迹 */
  x: boolean;
  /** 是否显示 Y 轴端点轨迹 */
  y: boolean;
  /** 是否显示 Z 轴端点轨迹 */
  z: boolean;
  /** 是否显示中心点位移轨迹 */
  center: boolean;
};
